# NovaSocial — Issue Index (master)

**Master index of ALL issue categories.** Synchronized whenever issue status changes, an issue is added, or a category file is created.
**Established:** 2026-09-07 (H11 task). **Protocol:** see ISSUE_RULES.md.
**Status legend:** FIXED (fixed + regression-tested, commit recorded) · OPEN · DEFERRED · IN_PROGRESS · WONT_FIX · DUPLICATE · SAFE (verified non-issue) · HUMAN-DECISION (owner call required, legacy rows)

---

## SECURITY_ISSUES.md

### FIXED (XSS H-series — esc-at-sink commits on Branch2)
| ID | Summary | Status |
|----|---------|--------|
| XSS-H1 | Chat message bodies/reply previews/sender names (load-msgs.js) | FIXED (92bb5d3) |
| XSS-H1b | Older-message scroll-up renderer (-load-older-messages.js) | FIXED (10a85bb) |
| XSS-H4 | Comment usernames + text (comments.js) | FIXED (d903268) |
| XSS-H5 | Notification sender names + payloads (notifications.js) | FIXED (53cc841) |
| XSS-H6/H6b | DM list name + last-message preview (+ refresh) | FIXED (8fcc2f4) |
| XSS-H7 | Chat header group name / peer username (open-chat.js) | FIXED (318b0ee) |
| XSS-H8/H8b | Pinned-message bars | FIXED (8042e59) |
| XSS-H9 | GC Info + share-sheet group names / member list | FIXED (e30d6ba) |
| XSS-H10 | Post-card username/location + share-sheet post preview | FIXED (6b6dbf4) |
| XSS-H11 | Reels username + caption (reels-renderer-owner.js:119/:122) | FIXED (d7becc7) |
| XSS-H12 | Home story-rail username (home.js:136) | FIXED (dbf00f9) |
| XSS-H13 | Story viewer header username + reply placeholder (render-sv.js:30/:195) | FIXED (H13 commit) |
| XSS-H15 | Note viewer author username + full note text (note-viewer-owners.js:28/:32) | FIXED (H15 commit) |
| XSS-H16 | Notes Bar text pills + usernames + own PLUS-slot pill + other-profile note pill (notes-bar.js:76/:86/:90 + profile-view.js:389) | FIXED (H16 commit) |
| XSS-H17 | Note reactors list reactor username + typed reaction emoji — targeted stored XSS vs note OWNER (note-reactors-list-owner.js:20/:21) | FIXED (H17 commit) |
| XSS-H18 | Profile/preview/full-profile/follow-list/story-viewers username+full_name — 11 HTML-text sinks (profile-view.js:88/:92/:156/:160/:276/:298/:302/:376/:403 + follow-list.js:34 + show-story-viewers.js:44) | FIXED (H18 commit) |
| XSS-H19 | Nova AI panel shared innerHTML sink (appendNovaMsg) + voice pipeline + command DB-username paths + translate modal ORIGINAL/TRANSLATED (nova-ai.js:162/:219, voice-assistant.js:139, nova-universe.js:104/:108) | FIXED (H19 commit) |
| SEC-002 | Story overlay poll question/options raw render to all story viewers (sv-append-overlays.js:44/:51 — H13-discovered; stored cross-user XSS via stories.overlay_data; fixed with esc() at both HTML-text output boundaries) | FIXED (SEC-002 commit) |
| XSS-M1 | Attribute/JavaScript-context hardening of unconstrained URL columns (media_url/avatar_url/cover_url/thumbnail_url/group_avatar) across 28 inventory sites / 17 files — esc() at src/poster/data-media-url/style-attr boundaries + encodeURIComponent data-attr constant-onclick transport at every onclick argument (load-msgs/posts/post-actions/reels/profile grids/home tray/profile-view cover+avatar+grids/explore/news-feed/admin-deleted/group surfaces + the -load-older twin + viewChatImage/viewAvatarFullscreen handler sinks); full record + M1-audit site additions in SECURITY_ISSUES.md §1.1 row + §15; the XSS-C9-audit-discovered cross-user site additions (universal-search.js:126 AI-search grid media_url/thumbnail_url + story-highlights.js:37 highlights.cover_url) FIXED 2026-09-13 by the dedicated universal-search/story-highlights task — SECURITY_ISSUES.md §24 | FIXED (M1 commit, 2026-09-09; site additions closed 2026-09-13) |
| XSS-M2 | Profile-view bio rendering — profiles.bio cross-user stored XSS: profile-view.js:135 preview `<`-only partial escape (esc-consistency) + :408 full-profile linkify(prof.bio) with NO escaping (all non-URL text raw → innerHTML); fixed with esc() + esc-then-linkify (H1's order — URL linkification preserved); parent 172c40d PROVEN VULNERABLE 96/0, post-fix focused 531/0; full record in SECURITY_ISSUES.md §1.1 row + §16 | FIXED (M2 commit, 2026-09-09) |
| XSS-M3 | Reaction badge stored-emoji XSS — message_reactions.emoji rendered raw (join(' ')) at all 3 render paths of the same badge: -update-message-reaction-in-place.js:17 (in-place refresh; ledger cite ":14" = 3-line drift) + load-msgs.js:153 (initial render, fires on every chat open) + -load-older-messages.js:111 (scroll-up pagination) — write paths client-side-constrained only (fixed emoji enum + window-global reactMsg), direct DB-write bypass = threat model → stored cross-user XSS vs the other chat party; fixed with esc() each emoji (the row's own prescription) — HTML-text context, plain-text content model; parent ee17031 PROVEN VULNERABLE 186/0 (14 tag payload classes × 3 sites mint elements, cross-user stored proven at all 3 delivery paths), post-fix focused 365/0; full record in SECURITY_ISSUES.md §1.1 row + §17 | FIXED (M3 commit, 2026-09-09) |
| XSS-M4 | Note music metadata + JSON onclick — esc() at every HTML-text/img-src sink + encodeURIComponent data-attr constant-onclick transport at every onclick argument, 6 files: note-viewer-owners.js:35-39 (viewer chip — the row's own site) + search-music-for-note.js:13-26 (H16 site) + render-note-music-section.js:8-11 (H16-recorded composer chip) + show-music-segment-picker.js:12-21 + render-recent-music-suggestions.js:13-19 (M4-audit site additions) + profile-view.js:389 music branch (H16-recorded, latent); stored cross-user XSS vs any note viewer (direct-DB-write threat model) + composer-side external iTunes metadata; parent 4fef0d9 PROVEN VULNERABLE 56/0 (tag mints at all 6 sites + the row's own ' -breakout vector with JS-compiling injected handlers + start_sec comma-operator injection + CSS url() breakout), post-fix focused 1059/0; full record in SECURITY_ISSUES.md §1.1 row + §18 | FIXED (M4 commit, 2026-09-09) |
| XSS-M5 | Admin approval decode-back — esc()'d usernames/report-reasons inside onclick JS-string-attribute contexts on admin action buttons: the HTML parser entity-decodes &#39; back to ' BEFORE the JS engine evaluates the attribute, re-closing the JS string (the .replace(/'/g,"\\'") after esc is a no-op — esc already consumed every literal '); 10 files / 24 sink lines: admin-tab-approvals.js:39/:40 + show-admin-user-detail.js:39/:40/:41 (the 1.3 row's own sites — 6 ternary branches) + load-admin-content.js:52/:53 + search-user-for-promotion.js:29/:30/:32 + load-appeals-list.js:24/:25 + load-team-list.js:56 + load-reports-list.js:120 (the ATTACKER-AUTHORED report reason) + load-verify-list.js:25/:26 + show-report-detail.js:165/:166/:216 + show-staff-actions.js:6/:15/:16/:20/:22 (the stage-2 continuation — the decoded runtime arg re-interpolated raw into the modal title [fresh modal.js:43 innerHTML] and 4 quote-replace-only onclicks; M5-audit site additions per the task's §3/§4 full-path mandate, dedupe rule #5, call-site map verified closed); stored CROSS-USER XSS targeting any admin/moderator viewer (signup-unvalidated profiles.username + attacker-authored reports.reason + direct-DB-write threat model — exploitation needs NO admin rights); fix = the row's own prescription option 2: encodeURIComponent data-attr + constant-expression onclick transport at every onclick argument (23 data-attr reads, zero attacker data inside any JS string, ids/dev-constants stay inline) + esc() at the stage-2 modal title; parent 8650a69 PROVEN VULNERABLE 372/0 (P06 x');alert(1);// decode-back fires at every payload-carrying button across 11 renderer variants + P05/P07 button-DoS breakage + stage-2 element minting [title zone] + \"-breakout + the \\ double-escape-inversion execution + honest inertness for encoded/scheme classes), post-fix focused 4130/0; full record in SECURITY_ISSUES.md §1.1 row + §19 | FIXED (M5 commit, 2026-09-09) |
| XSS-pre-audit wraps | H3 wrap series, 9 sites | FIXED (83633df…eea3a7a) |
| HA-H3 | Systemic XSS surface premise → wrap series | FIXED (8176eeb…) |
| HA-M4 | eval(a.action) in profile sheet → dispatch table | FIXED (df4261a) |

### OPEN / DEFERRED (security)
| ID | Summary | Status |
|----|---------|--------|
| XSS-M1 | media_url/avatar_url/cover_url in src/CSS-url/onclick contexts (posts/reels/profile grids + home tray :133 — H12 site; profile-view :392 viewAvatarFullscreen onclick — H16 site; profile-view :62/:267/:366 cover — H18 sites) | FIXED (M1 commit 2026-09-09 — row moved to FIXED above; see SECURITY_ISSUES.md §1.1 + §15) |
| XSS-M2 | profile-view bio partial escape (:135) + full-profile linkify(bio) unescaped (:408 — H18 site) | FIXED (M2 commit 2026-09-09 — row moved to FIXED above; see SECURITY_ISSUES.md §1.1 + §16) |
| XSS-M3 | Reaction badge stored emoji raw (in-place updater :17 + initial render load-msgs.js:153 + pagination -load-older-messages.js:111 — same-class site additions found by the M3 full-path audit) | FIXED (M3 commit 2026-09-09 — row moved to FIXED above; see SECURITY_ISSUES.md §1.1 + §17) |
| XSS-M4 | Note music metadata + JSON onclick (+ music search rows :13-25 — H16 site; + composer chip render-note-music-section.js:8-11 — H16-recorded; + segment picker :12-21 + recents :13-19 — M4-audit sites; + profile-view.js:389 music branch — H16-recorded, latent) | FIXED (M4 commit 2026-09-09 — row moved to FIXED above; see SECURITY_ISSUES.md §1.1 + §18) |
| XSS-M5 | Admin approvals esc'd-username-in-onclick decode-back (+ 7 admin-surface site additions + the show-staff-actions stage-2 continuation — M5-audit sites; + load-reports-list.js:120 the attacker-authored report-reason site) | FIXED (M5 commit 2026-09-09 — row moved to FIXED above; see SECURITY_ISSUES.md §1.1 + §19) |
| XSS-M6 | Own-profile render XSS — full_name/username display + @username + linkify(bio) unescaped + own note pill (:74 text+music branches — H16 site, 1-line drift from row's ":75") + own-website line :99 (M6-audit site addition, same own-profile self-XSS class — data-attr transport + esc'd display) | FIXED (M6 commit 2026-09-10 — row moved to FIXED above; see SECURITY_ISSUES.md §1.1 + §20) |
| XSS-M7 | Ban→Appeal chain status badges — client-insert-written ban_appeals.status / ban_approvals.status (submit-ban-appeal.js:10 + moderator-recommend-ban.js:12 send status:'pending' in the client INSERT payload — payload-tamperable by the hostile banned user, M1/M4/M5/H10-11 threat model) rendered RAW: load-appeals-list.js:20 ${a.status} (admin appeals list — the chain's high-value cross-user surface) + admin-tab-myapprovals.js:32 ${r.status} (staff list) → stored CROSS-USER XSS, zero-click alert(1) in the admin context; full 28-surface chain audit — every other sink inert at parent (appeal_reason/username/ban_reason/admin_notes esc'd, av() C1-hardened, M5 transports, notifications esc'd :184, toast/prompt text-only, UUID onclick args DB-typed); fix = esc() at both badge sinks (H18-twin, byte-identical legit enums); parent 97c8ecb PROVEN VULNERABLE 379/0 + negctl 33, post-fix focused 398/0; deferred same-class sites (reports.status :189/:32, audit status :112, media_type :42/:44) + RLS runbook items recorded — SECURITY_ISSUES.md §27; §27.4 CONTINUATION 2026-09-21 (section 28): the five deferred sites CLOSED — reports.status show-report-detail.js:189 + load-user-report-stats.js:32 (client-insert-written per submit-report.js:13), audit_logs.status admin-tab-audit.js:112 (render side esc'd; write side = external log_audit_entry RPC role-gating dependency, UNVERIFIED from repo), posts.media_type :42 + stories.media_type :44 (client-insert-written per submit-create.js:97/submit-story.js:136) — esc() at all five (H18-twin, byte-identical enums); parent 828abab proven vulnerable 362/0, post-fix focused 373/0, negctl 63; 3 NEW same-family deferred sites recorded §28.4 (load-reports-list.js:84/:89 media_type + admin-tab-audit.js:113 actor_role); §28.4 CONTINUATION 2026-09-22 (section 29): the three §28.4 sites CLOSED — messages.media_type load-reports-list.js:84 [${esc(m.media_type)} message] + stories.media_type :89 [${esc(s.media_type||'media')} story] (both CLIENT-INSERT-WRITTEN — messages ×7 insert sites incl. complete-forward-message verbatim copy, stories submit-story/publish-story-editor — cross-user delivery via any-user self-report → zero-click alert in admin Reports tab; classified A. CONFIRMED VULNERABLE) + admin-tab-audit.js:113 ${esc(a.actor_role)} roleBadge (audit_logs RPC-ONLY write side — log_audit_entry args carry NO actor_role param, role assigned server-side by the EXTERNAL RPC → classified D. UNVERIFIED-external, render hardened, dependency retained §29.4); parent 3a27250 proven vulnerable 231/0 + negctl 35, post-fix focused 261/0; NEW deferred: admin-tab-audit.js:125 target_id raw (8-char truncated, client-supplied p_target_id class, LOW) | FIXED (M7 commit 2026-09-19 + M7-§27.4 continuation 2026-09-21 + §28.4 continuation 2026-09-22) |
| XSS-C1 | av() first-letter + onerror JS-string + img src (all callers incl. reels :118, story-viewer :30, notes surfaces — H16 sites; profile/follow/viewer av sites — H18 sites; profile.js :76/:77 — M6-audit-confirmed C1 sites) | FIXED (2026-09-19 — shared helper hardened in utils.js: esc'd img src + data-fl constant-handler letter transport + esc'd letter span + safeName removed; ALL ~60 callers closed at once; cross-user M1-class img-src vector + letter breakage both closed — see SECURITY_ISSUES.md §26) |
| XSS-C2 | nova-ai own-msg partial escape (+ voice-assistant.js:139 same pre-escape) | FIXED (H19 commit — sink-level esc, pre-escape removed) |
| XSS-C3 | notes-bar own reaction badge (self-XSS) | OPEN (accepted low) |
| XSS-C5 | isSystem() styling spoof | OPEN (cosmetic) |
| XSS-C9 | notes.js personal localStorage notes raw render + nova-ultra-patches.js:46 moodChip currentMood (self-localStorage, ai-moderation.js:39 — H19-discovered site; + M1-audit own-data sites memories.js:65/avatar-action-sheet.js:11; + M6-audit own-data sites profile.js:52/:125/:238; + load-prof.js:27 — self-XSS only) | PARTIAL-FIXED (2026-09-12 XSS-C9 task: own-profile URL/media surfaces profile.js:52/:125/:238 + load-prof.js:27 FIXED — section 23 of SECURITY_ISSUES.md; notes.js:57-58 + nova-ultra-patches.js:46 + memories.js:65 + avatar-action-sheet.js:11 remain OPEN accepted-low self-XSS) |
| XSS-10.5 | esc() insufficient in JS-string-attr contexts (class) | OPEN |
| H9-D1 | JS-string/inline onclick class (openChat/initiateCall/sendSharedPostToChat/addToGroup/insertMention/shareText…) | OPEN (dedicated hardening task) |
| H9-D2 | Username-rendering surface class (mentions/call-UI/share-pickers/GC-add-member + close-friends/blocked-list/se-search-mention — H18 sites) | OPEN |
| H9-D3 | modal.js dynamic title caller audit (voice-rooms.js:53, show-staff-actions.js:6) | OPEN |
| H9-D4 | av() review | RESOLVED via XSS-C1 (FIXED 2026-09-19 — deduped into XSS-C1, closed by the C1 helper fix) |
| H10-3 | postCard → av() username (C1 class) | RESOLVED via XSS-C1 (FIXED 2026-09-19 — closed by the C1 helper fix) |
| H10-4 | postCard/shareSheet media_url (M1 class) | OPEN (merged into XSS-M1) |
| H10-5 | shareText onclick JS-strings (10.5 class) | OPEN (merged into H9-D1) |
| H10-6 | memories.js own @username + caption (self-XSS) | OPEN |
| H10-7 | scheduled-posts.js own localStorage caption (self-XSS) | OPEN |
| H10-8 | insights.js own @username (self-XSS) | OPEN |
| H10-9 | trending.js hashtag name DB-write bypass | OPEN |
| H10-10 | explore doSearch PEOPLE raw | OPEN |
| H10-11 | universalAISearch PEOPLE raw (story-highlights.js:38/:39 title site addition FIXED 2026-09-13 — see SECURITY_ISSUES.md §24; core sites :113/:114/:115 [username + full_name + bio slice] FIXED 2026-09-15 — see §25; adjacents classified: :111 av() = XSS-C1 row, :110 id = UUID-safe, :114 fmt(followers_count) = numeric-safe per H11-audit row #9) | FIXED (2026-09-15, core sites; 2026-09-13, site addition) |
| H10-12 | collaboration picker username + selectCollab onclick | OPEN |
| H10-13 | admin deleted-posts @username raw | OPEN |
| H10-1 | share-sheet post-preview @username | FIXED (6b6dbf4) |
| H10-2 | share-sheet post-preview caption | FIXED (6b6dbf4) |
| SEC-001 | reels error-path e.message raw render (H11-discovered, defense-in-depth; home feed error paths H12-added; profile-preview error path H18-added) | OPEN |
| SEC-002 | story overlay poll question/options raw render to all story viewers (sv-append-overlays.js:44/:51 — H13-discovered) | FIXED (SEC-002 commit — row moved to FIXED above; see SECURITY_ISSUES.md section 14) |
| SEC-003 | story editor poll live-preview self-XSS (story-poll-addon.js:224/:228 — author's own input only; SEC-002-audit discovery) | FIXED (SEC-003 commit 2026-09-12 — esc() wraps at updatePollPreview :224/:228 mirroring the editor-canvas/viewer patterns; self-XSS threat model re-verified; see SECURITY_ISSUES.md section 22) |
| SEC-004 | profiles.website cross-user stored XSS (profile-view.js:136→:164 preview display + :348→:409 full display + single-quoted JS-string onclick; sanitizeUrl scheme-allow-list hardening in utils.js; own-profile twin profile.js:99 policy-honoring condition — FIXED at all 4 sites by the dedicated SEC-004 task 2026-09-12: esc() displays + M6 data-web constant-onclick transport + `return ''` allow-list fallthrough + browser-faithful normalization; see SECURITY_ISSUES.md section 21) | FIXED (SEC-004 commit — row moved to FIXED above; see SECURITY_ISSUES.md section 21) |
| XSS-C4 | rename input quote-escaped attr | SAFE |
| XSS-C6 | reactionMap[...] claimed bug | SAFE (non-issue) |
| XSS-C8 | settings share-link constant | SAFE |

## BUG_ISSUES.md

| ID | Summary | Status |
|----|---------|--------|
| HA-H1 | smart-reply dead wiring (cinp→minp) | FIXED (26ffd03) |
| HA-H2 | video-length picker containers missing | FIXED (7c395e6) |
| HA-M1 | notification badge chain dead | FIXED (5b8ea7a) |
| HA-M2 | comment moderation pre-check dead (cinp→ci-<pid>) | FIXED (1e422fa) |
| HA-M3 | following-count stat wiring missing | FIXED (60287fb) |
| DG-1 | _origCheckUnread dead guard repaired | FIXED (6bf3db0) |
| DG-2 | _origSendCmt dead guard repaired | FIXED (3e29452) |
| DG-3 | _origInitNova double-blocked; activation leaks 60s intervals | OPEN (HUMAN-DECISION) |
| DG-4 | updateMyInterests never runs on Branch2 (load order) | OPEN (HUMAN-DECISION) |
| DG-5 | 3 more dead v2 guards in nova-ultra-patches.js | OPEN |
| DG-6 | sendCmt stale-id historical | FIXED (subsumed) |
| H9-D5 | add-to-group.js conversations.name column bug (cosmetic) | OPEN |

## PLATFORM_ISSUES.md

| ID | Summary | Status |
|----|---------|--------|
| HA-M5 | sw.js cache name never versioned (novasocial-v1) + cache-first policy — stale-module risk after deploys | DEFERRED (deploy-gated, owner decision) |

## CODE_HYGIENE_ISSUES.md

| ID | Summary | Status |
|----|---------|--------|
| HA-M6 | feature scaffolding dirs (14 empty) — reorganization decision | OPEN (decision) |
| HA-hygiene (junk root file) | 1-byte junk artifact removed | FIXED (7a1cc80) |
| HA-M7 | addStoryTextMode dead function removed | FIXED (7a1cc80) |
| HA-L4 | react-box no-op removals | FIXED (7a1cc80) |
| HA-L1 | pairing-contract filename drift | FIXED (7a1cc80) |
| HA-L2 | readiness assertion-message drift | FIXED (7a1cc80/6615800) |
| HA-L3 | safety-harness LATEST_CHECKPOINT label lags | OPEN (cosmetic) |
| HA-L5 | nav-debug utilities kept deliberately | WONT_FIX (kept) |
| H10-14 | av() dead safeName variable | RESOLVED via XSS-C1 (FIXED 2026-09-19 — safeName removed by the C1 fix; was deduped into XSS-C1) |
| HYG-001 | dblLikeReel declared twice (global + nested in renderReels) — H11-discovered | OPEN |

## UI_UX_ISSUES.md

| ID | Summary | Status |
|----|---------|--------|
| H10-15 | formatCaption double-escape `&`-display quirk after "more" | OPEN |

---

## Category files NOT yet created (no known issues of that category yet)

PERFORMANCE_ISSUES.md · ARCHITECTURE_ISSUES.md · DATABASE_ISSUES.md · REALTIME_NOTIFICATION_ISSUES.md · AI_ISSUES.md · DEPLOYMENT_ISSUES.md

Per ISSUE_RULES.md #9, a category file is created only when an existing or newly discovered issue belongs to that category. (Historical note: HA-M1 was classified BUG by root cause — stale DOM-ID wiring — even though the broken feature is notifications; DG-3's interval leak is also wiring, not realtime.)

## Synchronization log

- 2026-09-07 (H11): index created with full historical import (migration from docs/SECURITY_DEFERRED_ISSUES.md); XSS-H11 → FIXED; SEC-001, HYG-001 added; XSS-M1/XSS-C1 site additions recorded.
- 2026-09-07 (H12): XSS-H12 → FIXED (home.js:136 esc); XSS-M1 site addition (home.js:133 tray avatar img src); SEC-001 site additions (home.js:430/:442 feed error paths); "C7" dangling reference clarified → XSS-C1 (no row deleted/merged); no new issues, no new category files.
- 2026-09-07 (H13): XSS-H13 → FIXED (render-sv.js:30/:195 esc — header username + reply placeholder); NEW issue SEC-002 (story overlay poll content raw — sv-append-overlays.js:44/:51, HIGH, OPEN, future task); XSS-C1 site addition (render-sv.js:30 av() call); no category files created.
- 2026-09-07 (H15): XSS-H15 → FIXED (note-viewer-owners.js:28/:32 esc — author username + full note text; ledger line refs clarified :29/:33 → actual :28/:32, 1-line counting drift, no content change); branch2-only-safety-contract-harness allowlist admission for note-viewer-owners.js; no new issues, no site additions, no category files created.
- 2026-09-08 (H16): XSS-H16 → FIXED (notes-bar.js:76/:86/:90 esc — own PLUS-slot pill + others' pill + others' username; profile-view.js:389 esc — other-profile active-note pill text branch, XSS-H16 family site per dedupe rule #5); NEW issue XSS-C9 (notes.js:57-58 personal localStorage notes self-XSS, LOW, accepted low); site additions — XSS-M1 (profile-view.js:392 viewAvatarFullscreen onclick), XSS-M4 (search-music-for-note.js:13-25), XSS-M6 (profile.js:75 own-profile note pill), XSS-C1 (notes surfaces), XSS-C3 line-ref clarified :84 → actual :88; no category files created; TRACK A audit conclusion recorded (no href sink in Notes Bar; pre-fix breakout minted executable <a href=javascript:> elements — fixed by same esc sinks).
- 2026-09-08 (H17): XSS-H17 → FIXED (note-reactors-list-owner.js:20/:21 esc — reactor username + typed reaction emoji; targeted stored XSS executing against the note OWNER, container gated behind isOwnNote at note-viewer-owners.js:41); branch2-only-safety-contract-harness allowlist admission for note-reactors-list-owner.js; note-reactors-list-production-split-contract-harness parity re-pinned to origin/main + exactly the H17 esc delta; no new issues, no site additions, no category files created; write-path constraint documented (reactToNote upsert unvalidated — maxlength=4 client-side only, receiver-side esc is the only defense).
- 2026-09-08 (H18): XSS-H18 → FIXED (profile-view.js:88/:92/:156/:160/:276/:298/:302/:376/:403 + follow-list.js:34 + show-story-viewers.js:44 esc — profile/preview/full-profile/blocked-shell username+full_name, followers/following rows, story-viewer rows; 11 HTML-text sinks, widest username exposure; write path = settings.js saveEdit client-side-only username regex + NO full_name validation); branch2-only-safety-contract-harness allowlist admission for follow-list.js (ledger-prescribed); H16 verify-suite L.3 evolution-proofed (scripts/ outside repo — historical H16 delta re-anchored to immutable 541196a + stricter L.3b sanction-check, product code unchanged); site additions to existing class rows (no new IDs): XSS-M2 (profile-view.js:408 linkify(bio) unescaped), XSS-M1 (profile-view.js:62 cover CSS-url + :267/:366 cover src), XSS-C1 (profile/follow/viewer av sites), SEC-001 (profile-view.js:213 error path), H9-D2 (close-friends.js:46, show-blocked-list.js:15, se-search-mention-users.js:48/:51); dedupe recorded (profile-view :403 absorbed into H18; explore/universal-search stay H10-10/H10-11; H19 + SEC-002 untouched).
- 2026-09-08 (H19): XSS-H19 → FIXED (nova-ai.js:162 esc() at the shared appendNovaMsg sink covering all 8 call sites — typed + voice pipelines, AI/API response, local fallback, command DB-username paths, sensitive canned, user echo; :219 + voice-assistant.js:139 pre-escape removal for single escaping stage; nova-universe.js:104/:108 translate modal ORIGINAL posts.caption + TRANSLATED GLM-response esc; content model = PLAIN TEXT by runtime audit — no Markdown/sanitizer/streaming/persistence exists); XSS-C2 → FIXED (absorbed as its row prescribed); branch2-only-safety-contract-harness allowlist admission for nova-ai.js + voice-assistant.js (ledger-prescribed); site addition: XSS-C9 gains nova-ultra-patches.js:46 moodChip + ai-moderation.js:39 (currentMood self-localStorage class, deferred); nova-universe.js:104 recorded as a posts.caption-class site (H10-2 family, fixed in H19); no new issue IDs, no category files created; with H19 the H1-H19 HIGH XSS backlog is fully closed — remaining HIGH = SEC-002 only.
- 2026-09-08 (SEC-002): SEC-002 → FIXED (sv-append-overlays.js:44/:51 esc() — story overlay poll question + option text, stored cross-user XSS via stories.overlay_data, esc at both HTML-text output boundaries; parent e93136f negative-control-proven vulnerable 56/0, post-fix 228/0; row moved 1.2 OPEN → 1.1 FIXED with full record, SECURITY_ISSUES.md section 14 added, hooks renumbered 14→15; NO harness changes required — sv-append-overlays.js already allowlisted); NEW SEC-003 (story-poll-addon.js:224/:228 live-preview self-XSS, LOW, deferred — no cross-user vector); with SEC-002 closed the audited HIGH XSS backlog H1-H19 + SEC-002 is FULLY CLOSED — remaining HIGH: none in this backlog; deferred M/L/class issues unchanged (M1-M6, XSS-C9, SEC-003, JS-string class, username-rendering class, av() review, modal-title audit, DG-3/4/5, HA-M5, SEC-001, CSS-context + ov.url residuals same-row).
- 2026-09-09 (M1): XSS-M1 → FIXED (attr-safe esc() at every src/poster/data-media-url/style-attr boundary + encodeURIComponent data-attr constant-onclick transport at every onclick argument — 17 files, 35 sites; write-path provenance + full site inventory in SECURITY_ISSUES.md §15.1; parent 1e66272 negative-control-proven vulnerable 812/0 with 40 sink-shape controls, post-fix focused 894/0; row moved 1.3 OPEN → 1.1 FIXED with all site-addition history preserved; H10-4 ":151" cite clarified); site additions to existing class rows per dedupe rule #5 (no new IDs): XSS-10.5 gains show-report-detail.js:113/:134 (esc'd media_url in JS-string), XSS-C9 gains memories.js:65 + load-prof.js:27 + avatar-action-sheet.js:11 (own-data self-XSS sites), XSS-C1 gains an M1 note (av()-adjacent direct interpolations fixed; av() internal scope untouched per owner instruction); in-repo harness maintenance: view-chat-image/view-avatar-fullscreen marker sync + news-feed esc-count 2→3 + reels seam/navigation authorized-escape lists extended with the 3 M1 substitutions + branch2-only-safety allowlist admission (view-chat-image.js, view-avatar-fullscreen.js, news-feed.js); 322 regression 317/5 byte-identical to baseline; app-load 10/10; all prior H/SEC-002 suites re-verified green; with M1 closed: 5 MEDIUM remain (M2-M6) + documented LOW/class residuals.
- 2026-09-09 (M2): XSS-M2 → FIXED (profile-view.js:135 esc() — preview bio partial-escape → esc-consistency + :408 linkify(esc(prof.bio)) — esc-then-linkify, H1's order, URL linkification preserved; 1 file, 2 lines; parent 172c40d negative-control-proven vulnerable 96/0 — 14 payload classes minted elements+handlers at :408, :135 proven double-decode/consistency gap; post-fix focused 531/0 — every payload inert, anchors audited, multilingual+double-escape clean, linkification functional); row moved 1.3 OPEN → 1.1 FIXED with all history preserved; section 16 added, hooks renumbered 16→17; NO new issue IDs, NO site additions (adjacent bio surfaces deduped: profile.js:98 = XSS-M6, universal-search.js:115 = H10-11, :213 = SEC-001); suite-tooling outside repo only (H18 F25 re-pointed + S-H 18; H16 L.3b M2 shapes + 18); NO in-repo harness changes; 322 regression 317/5 byte-identical baseline; app-load 10/10; all prior H/SEC-002/M1 suites re-verified green; with M2 closed: 4 MEDIUM remain (M3-M6) + documented LOW/class residuals.
- 2026-09-09 (M3): XSS-M3 → FIXED (message_reactions.emoji esc'd per emoji at all 3 reaction-badge render sites: -update-message-reaction-in-place.js:17 [in-place refresh — the ledger row's cited site, ":14" = 3-line counting drift] + load-msgs.js:153 [initial render] + -load-older-messages.js:111 [scroll-up pagination — both same-class site additions per the task's §3C/§8 full-path mandate, dedupe rule #5; NO XSS row previously existed for the badge zone in those files]; 3 files, 3 sink lines, 5 insertions/3 deletions; write paths = client-side fixed-enum only + window-global reactMsg + direct DB-write bypass (RLS unknown per audit §16) → stored CROSS-USER XSS vs the other chat party, delivered on every chat open / scroll-up / own-reaction refresh; parent ee17031 negative-control-proven vulnerable 186/0 — 14 tag payload classes × 3 sites mint real parsed elements with execution signatures [on* handlers, script bodies, javascript: src], stored-shape JSON + malformed + 3KB payloads mint, cross-user stored proven at all 3 delivery paths, quote/entity/scheme-pure payloads honestly context-inert at this HTML-text sink; post-fix focused 365/0 — every payload × every site inert [zero census drift, zero payload-carrying handlers, zero dangerous href/src], multilingual 8 languages byte-exact, double-escape single-stage, all 5 badge branches + join separator + nullish-emoji rows functional, H1/H1b benign-reaction contract preserved); row moved 1.3 OPEN → 1.1 FIXED with all history preserved; section 17 added, hooks renumbered 17→18; NO new issue IDs (adjacent reaction surfaces deduped: notes-bar.js:88 = XSS-C3, note-reactors-list = H17 FIXED, trending.js = H10-9); NO in-repo harness changes (no harness pins the reaction zone — reactionPop grep = 0 hits; 3 files already allowlisted; H1/H1b benign-reaction assertions esc-identity-safe); M3 verify harness lives outside repo in scripts/; 322 regression 317/5 — failing set identical to documented baseline (branch2-final-readiness mid-task dirty-worktree artifact re-verified post-commit); app-load 10/10; all prior H/SEC-002/M1/M2 suites re-verified green; with M3 closed: 3 MEDIUM remain (M4-M6) + documented LOW/class residuals.
- 2026-09-09 (M4): XSS-M4 → FIXED (note music metadata + JSON onclick — 6 files / 15 sink lines: note-viewer-owners.js:35-39 viewer chip [the 1.3 row's own site: title/artist raw HTML text + artwork raw img src + JSON.stringify(preview_url)/music_start_sec raw inside a single-quoted onclick] + search-music-for-note.js:13-26 [the 1.3 row's H16 site addition: trackName/artistName raw text + artworkUrl60 raw src + 3 JSON.stringify onclicks] + render-note-music-section.js:8-11 [H16 §10-recorded composer chip] + show-music-segment-picker.js:12-21 + render-recent-music-suggestions.js:13-19 [M4-audit site additions per the task's §3 full-path mandate — music picker + local state, dedupe rule #5] + profile-view.js:389 music fallback branch [H16 fix-note-recorded "M4 class", latent — checkUserActiveNote selects id,text only]; fix = the row's own prescription: esc() at every HTML-text/img-src sink + the M1-established encodeURIComponent data-attr constant-expression onclick transport for every JSON/onclick argument (zero attacker data inside JS strings; music_start_sec unary-+ coerced, NaN→0; CSS url() style-attr esc'd with the M1 :62-class entity-decoded CSS-string residual documented same-row); write path = iTunes API → _noteMusic → quick_notes.music_* verbatim (escape-at-sink, unchanged) + direct-DB-write bypass (RLS unknown per audit §16) → stored CROSS-USER XSS vs any note viewer (harness models attacker-owned note → victim sandbox); composer-side sites = semi-trusted external iTunes metadata + self-localStorage recents; parent 4fef0d9 negative-control-proven vulnerable 56/0 — tag payloads mint elements with on* handlers at all 6 sites, the row's OWN ' breakout vector proven with injected handler values that COMPILE as valid JS (alert(1);// + template tail), music_start_sec string makes the onclick itself comma-operator-executable, CSS url() ' breakout injects overlay CSS, JSON.stringify honestly proven JS-safe for " (the vulnerability is exactly the HTML single-quote boundary), the pill's 14-char slice window honestly reported structure-only; post-fix focused 1059/0 — every payload × field × site inert (zero census drift, constant onclicks byte-asserted, data-attr round-trips, esc'd srcs round-trip, multilingual 9 languages byte-exact, double-escape single-stage, all music functionality F1-F15 preserved, boundary counters zero, 8 handler/persistence files byte-identical to parent); row moved 1.3 OPEN → 1.1 FIXED with all history preserved; section 18 added, hooks renumbered 18→19; NO new issue IDs (adjacent music surfaces deduped: profile.js:74 = XSS-M6 self-XSS, load-notes-feed.js:72 already esc'd, notes-bar.js:77/:87 constant only, story-music-helpers = stories family safe [dev constants + textContent], Audio() usage documented safe-by-construction); NO in-repo harness changes (all music-zone contract harness pins verified compatible BEFORE the fix: _noteMusic.artwork/title/artist||'' + togglePreviewPlay( + selectNoteMusicResult( + song.artwork||"" + confirmMusicSegment( + artwork.replace('60x60','300x300') + toggleSegmentPreview( + segment-play-btn + setupSegmentDragWindow + :78 autoplay call + handler signatures + allowlist + onclick-resolution — all retained; M4 verify harness lives outside repo in scripts/; H15/H16/H18/M2 outside-repo verify suites re-anchored to the M4-fixed shapes with documented M4-evolution comments, product code unchanged); 322 regression 317/5 — failing set identical to documented baseline (parent-state git-stash signature comparison: 4/5 byte-identical, branch2-final-readiness mid-task dirty-worktree artifact re-verified post-commit); app-load 10/10; all 10 boundary harnesses exit 0; all prior H/SEC-002/M1/M2/M3 suites re-verified green; with M4 closed: 2 MEDIUM remain (M5-M6) + documented LOW/class residuals.
- 2026-09-09 (M5): XSS-M5 → FIXED (admin approval decode-back — 10 files / 24 sink lines: admin-tab-approvals.js:39/:40 + show-admin-user-detail.js:39/:40/:41 [the 1.3 row's own sites, 6 ternary branches] + load-admin-content.js:52/:53 + search-user-for-promotion.js:29/:30/:32 + load-appeals-list.js:24/:25 + load-team-list.js:56 + load-reports-list.js:120 [the ATTACKER-AUTHORED report reason class site] + load-verify-list.js:25/:26 + show-report-detail.js:165/:166/:216 [M5-audit stage-1 site additions per the task's §3/§4 full-path mandate, dedupe rule #5] + show-staff-actions.js:6/:15/:16/:20/:22 [the stage-2 continuation: the decoded runtime username re-interpolated raw into the fresh-modal title (modal.js:43 innerHTML, the task §4 ENCODED→DECODE→RAW-HTML pattern) and 4 quote-replace-only onclicks]; the vulnerability = esc()'d attacker data in single-quoted JS strings inside double-quoted onclick attrs — the HTML parser entity-decodes &#39; back to ' BEFORE JS evaluation, re-closing the JS string (the .replace(/'/g,"\\'") after esc is a NO-OP); write path = signup-unvalidated profiles.username (auth.js:51 has NO charset regex) + attacker-authored reports.reason + direct-DB-write bypass (RLS unknown per audit §16) → stored CROSS-USER XSS targeting any admin/moderator viewer — exploitation needs NO admin rights; fix = the row's own prescription option 2, the M1/M4-established transport: encodeURIComponent data-attr + constant-expression onclick at every argument (23 data-attr reads, ZERO attacker data inside any JS string, UUID/dev-constant args stay inline + the approval-harness-pinned id-prefix shape retained) + esc() at the stage-2 modal title + handlers verified terminal-safe (prompt/confirm/toast-textContent, 20 handler files byte-identical); parent 8650a69 negative-control-proven vulnerable 372/0 — P06 x');alert(1);// decode-back EXECUTES at every payload-carrying button across 11 renderer variants, P05/P07 ' -breakouts = button DoS (the audit's own breakage wording), stage-2: tag payloads mint parsed elements + on* handlers in the modal title zone, the \" breakout mints onerror on the action buttons, and the \\ double-escape-inversion EXECUTES alert(1) at the quote-replace-only sites; honest inertness: quote-free tags ride inert as args (prompt/confirm native sinks), URL-encoded/double-encoded/scheme/JSON/fullwidth classes context-inert (no URL/JSON decode in this flow); post-fix focused 4130/0 — every payload × site inert (census zero drift, onclicks byte-identical to benign [constant expressions], 23 transports, data-attr round-trips EXACT incl. 3KB, multilingual 9 languages byte-exact, double-escape single-stage, admin flows F1-preserved, 20 handler files + modal.js + utils.js byte-identical to parent); row moved 1.3 OPEN → 1.1 FIXED with all history preserved; section 19 added, hooks renumbered 19→20; NO new issue IDs (adjacent surfaces deduped: non-admin JS-string sites = H9-D1/10.5 class [post-actions/dms/open-chat/search-add-member/show-call-history — untouched, dedicated task], show-report-detail :113/:134 = 10.5 site additions, posts.js:21 = H10-15 cosmetic, avatar-action-sheet = C9, show-create-story/smart-replies = dev constants safe, av() dead safeName = C1/D4, modal.js:43 = the deferred modal-title caller audit class [modal.js untouched]); NO in-repo harness changes (all admin contract harness pins verified compatible BEFORE the fix: approval id-prefix + labels + esc'd displays, user-detail labels + queries, promotion debounce + queries, moderation-admin markers, window-assignment names, allowlist, onclick-resolution — all retained; M5 verify harness lives outside repo in scripts/); 322 regression 317/5 — failing set identical to documented baseline (parent-state git-stash signature comparison: 4/5 byte-identical, branch2-final-readiness mid-task dirty-worktree artifact re-verified post-commit); app-load 10/10; all 10 boundary harnesses exit 0; all prior H/SEC-002/M1/M2/M3/M4 suites re-verified green; with M5 closed: 1 MEDIUM remains (M6) + documented LOW/class residuals.
- 2026-09-10 (M6): XSS-M6 → FIXED (own-profile render XSS — 1 file / 5 sink lines: profile.js :88-89 display-name full_name/username fallback + :96 @username [esc()] + :98 bio [linkify(esc(PROF.bio)) — esc-then-linkify, the row's own prescription "H1's order", the M2 profile-view.js:408 precedent] + :74 own active-note pill text+music branches [esc() — byte-exact mirror of the H16/M4 profile-view.js:389 expression; music branch LATENT, check-user-active-note.js selects only id,text] + :99 website [M6-audit same-class site addition — data-web encodeURIComponent transport + constant-expression onclick window.open(decodeURIComponent(this.dataset.web),'_blank') + esc'd display, the M1-established pattern; sanitizeUrl retained as the approved URL mechanism]; classification RE-AFFIRMED stored self-XSS — receiver = profile owner only, own row .eq('id',ME.id); write path = settings.js saveEdit [username regex client-side-only, full_name/bio/website NO validation]); parent 96e577c negative-control-proven vulnerable 534/0 (P01-P05/P15 mint parsed B/SCRIPT/IMG + surviving onerror handlers at the text sinks; S5 website P04 "-breakout mints onerror+IMG, P06 x');alert(1);// JS-string breakout EXECUTES alert(1) on click, P08 javascript: passed raw to window.open, P16 entity decode-back corrupts the window.open arg; S3 bio BV1 mints an executable-href javascript: anchor, BV2 linkify partial-match + raw tail mint; pill truncated-tag element minting — honest handler-truncation reporting; P16 double-decode display artifact); post-fix focused 1405/0 (strict census zero drift [tag multiset + handler VALUE triples], decoded zones round-trip every payload exactly, data-web round-trips EXACT, linkify anchors http/https only with zero javascript:/data:/vbscript: hrefs, mailto = pre-existing bare-domain branch, multilingual 9 languages byte-exact, double-escape single-stage, G6 functional F1-F12 [profile opens, 4 queries, all zones, profTab posts/saved/liked, fallbacks, no-note branch, localStorage cover, idempotence, error path], whole-template + per-segment byte-identity parent/disk for benign profiles, 10 adjacent files byte-identical, D-group documents the deferred C9 sites minting identically parent/disk); row moved 1.3 OPEN → 1.1 FIXED with all history preserved; section 20 added, hooks renumbered 20→21; NEW SEC-004 (HIGH, OPEN — profiles.website cross-user raw render at profile-view.js:136→:164 preview + :348→:409 full display/onclick JS-string: sanitizeUrl is a scheme-prefix filter NOT an escaper [its `return u` fallthrough passes javascript:/data:/markup raw — the audit doc's "allow-lists http/https" description ≠ implementation]; stored cross-user XSS, any profile viewer; discovered by the M6 website-family audit, NOT fixed per task §26 bounded-scope rule; own-profile twin :99 fixed in M6); XSS-C9 site additions (profile.js:52 own cover src + :125 own initial posts-grid src + :238 own nav-avatar onerror first-letter — own-data accepted-low family); XSS-C1 note (profile.js:76/:77 av() sites confirmed C1-class, untouched); NO in-repo harness changes required (profile.js already in the branch2-only-safety allowlist; no harness pins the M6 sink lines — profile-customizer pins only code-shape strings untouched by the fix); M6 verify harness lives outside repo in scripts/; with M6 closed: the audited MEDIUM M1–M6 backlog is FULLY CLOSED — remaining security work = SEC-004 (HIGH) + documented deferred LOW/class residuals.
- 2026-09-12 (SEC-004): SEC-004 → FIXED (the last remaining HIGH row — profiles.website cross-user stored XSS on profile-view.js: 2 render sinks [:136→:164 preview RAW display — zero-click markup minting; :348→:409 full-profile RAW display + the URL inside a single-quoted JS-string inside a double-quoted onclick window.open('${safeWebsite}','_blank') — ' breakout EXECUTES on click, " breakout mints handlers] + the sanitizeUrl() policy hole [utils.js `return u` fallthrough passed javascript:/data:/vbscript:/case-variants/whitespace/control-char smuggles raw — a scheme-prefix filter, NOT an escaper; the function's own 'Only allow http/https' comment never matched the implementation] + the profile.js:99 twin's policy exposure; write path = settings.js saveEdit website field NO validation + DB-write bypass; receiver = ANY profile viewer); fix = task §4 layer separation: esc() at both display sinks + the M1/M6-established data-web encodeURIComponent constant-expression onclick transport at :409 (window.open(decodeURIComponent(this.dataset.web),'_blank') — zero attacker data in the JS string) + sanitizeUrl hardened to a true scheme allow-list (String(url).replace(/[\t\n\r]/g,'').trim() + case-insensitive startsWith checks + return '' fallthrough — every previously-allowed value returns IDENTICALLY, verified input-by-input: no weakening; file/content extraction + bare-domain upgrade branches otherwise untouched; mailto:/arbitrary text now rejected by the allow-list — a website field renders websites) + profile.js:99 condition sanitizeUrl(PROF.website)? (blocked values hide the row instead of an empty about:blank-clickable div; M6 transport/onclick/display expressions byte-preserved) + branch2-only-safety allowlist admission of src/core/utils.js (M1 precedent); parent 8652c88 negative-control-proven vulnerable 117/0 (30-payload matrix × both sinks: markup minting with surviving on* handlers ZERO-CLICK on preview open; P04/P12/P25/P27 "-attr-breakout handler-VALUE drift; P06/P26/P28 ' JS-string breakouts EXECUTE alert(1) on click [P26/P27 prove quote payloads INSIDE https:// URLs and bare-domain upgrades defeat the old filter]; P08/P18/P19/P20/P21/P22 javascript:/data:/vbscript:/mixed-case/whitespace/control-char schemes reach window.open raw; P16b java&#x73;cript: entity decode-back delivers javascript:alert(1) to window.open; honest classes: P07 breakage-not-execution, P13 backslash-as-URL, P09-P11/P14 context-inert no-decode-step; P17/P29/P30 benign negative controls work); post-fix focused 438/0 (every payload × both sinks inert: policy-blocked → div ABSENT + zero census drift vs website-less controls + no window.open; allowed → data-web transport exact with zero attr-breakout chars + onclick byte-identical to benign [constant-expression proof] + window.open EXACT decode round-trip + esc-exact single-stage display round-trip; G2 policy suite: allowed values parent-identical [no weakening], 24 blocked scheme/case/ws/ctrl/entity variants → '', file-extraction parent-identical, two-slash file:// parent-RAW → disk-blocked [strengthening]; G3 functional F1-F10 [preview/full open, website display+transport+_blank+exact URL, blocked shell, error paths, idempotence, funnel, URL variants, posts grid, zero listeners]; G4 multilingual byte-exact + single-stage double-escape + benign byte-identity outside the website div; G5 hygiene + bypass search [no raw window.open('${ remains, counts exact, utils diff CONFINED to sanitizeUrl, M6 pins preserved, no equivalent sink remains]); 322 regression 317/5 baseline-identical [4/5 signatures byte-identical, branch2-final-readiness = the documented mid-task artifact, re-verified post-commit]; app-load 10/10; secret scan CLEAN; all prior suites green post-fix [H1-H19, SEC-002, M1 894/0, M2 531/0, M3 365/0, M4 1059/0, M5 4129/0, M6 1382/0 + prove 531/0]; suite-tooling evolutions OUTSIDE repo only, each with SEC-004-evolution comments: M6 harness era-faithful utils injection, H18 S-F website carve-out + diff bound 21, H16 L.3b +2 authorized lines, H4/H10/H11/SEC-002/M2/M5 esc-slice/diff-bound/allowlist-pattern updates; in-repo harness change = the branch2-only-safety allowlist admission ONLY [committed with the fix]; with SEC-004 closed: ZERO HIGH rows remain — the full audited backlog H1-H19 + SEC-002 + M1-M6 + SEC-004 is CLOSED; remaining security work = documented deferred LOW/class residuals only [SEC-001, SEC-003, C1, C9, H9-D1/10.5, H9-D2, modal-title audit, DG-3/4/5, HA-M5, cosmetics]).
- 2026-09-12 (SEC-003): SEC-003 → FIXED (the story editor poll live-preview self-XSS — story-poll-addon.js updatePollPreview ${q} :224 / ${o} :228 rendered the author's own question/option text RAW into preview.innerHTML on every keystroke via the REAL oninput wiring; threat model re-verified per the task §4 7-point check: SELF-XSS ONLY, LOW preserved — all state writers = author's own input events + static _POLL_TEMPLATES constants [grep-proven: the globals exist in exactly index.html + story-poll-addon.js; NO fetch/db/JSON.parse/storage in the module]; poll content IS persisted to overlay_data but every cross-user surface already esc()s it [viewer sv-append-overlays.js:44/:51 since SEC-002, editor canvas story-editor-owners.js:25/:28]; reel-poll.js re-confirmed a UI stub; the preview was the ONLY raw sink in the whole poll flow); fix = the row's own prescription: esc(q)/esc(o) — exactly 2 lines, 2 insertions/2 deletions, byte-for-byte the SEC-002/editor-canvas fix shape, no refactor; parent 69303c2 (the SEC-004 commit) negative-control-proven vulnerable 198/0 (19/19 markup payloads mint real parsed attacker DOM at the question sink, 12 with EXEC-class handlers [img-onerror/svg-onload/input-autofocus]; both option slots 19/19; mixed both-slots+question and 5-slot simultaneous runs mint from every field; long-markup-5k mints 254 elements; honest inertness: quote/JS-string/backslash classes are text-inert at this HTML-text sink even in the parent — no attribute/JS-string context to break out; entity payloads decode to text); post-fix focused 272/0 (every payload × both sinks inert — zero minted/handlers/URLs/EXEC classes; single-stage round-trip; multilingual 10-language + emoji byte-exact; long strings stable; benign byte-identity parent-vs-disk); functional F1-F13 all pass (default preview, fallback placeholders, live typing, template apply, style select, multivote toggle, add/remove option + counts + layouts + max-5, sePollCreate push/render/close/toast, rejection path, Hindi+emoji round-trip, modal close); 322 regression 317/5 baseline-identical [4/5 byte-identical parent signatures, branch2-final-readiness = the documented mid-task dirty-worktree artifact resolving post-commit]; app-load 10/10; 5 boundary + 8 DOM/security/realtime contract harnesses exit 0; secret scan CLEAN; all prior suites green [H1-H19, SEC-002 228/0, M1-M6, SEC-004 438/0]; sanctioned outside-repo suite-tooling evolution: the SEC-002 suite's 3 stale pins asserting SEC-003 was deferred → SEC-003-FIXED shapes with evolution comments; NO in-repo harness changes (story-poll-addon.js not pinned by any docs harness; already allowlisted); NO new issue IDs (full-path audit re-confirmed the 2 known sinks; no site additions); with SEC-003 closed: the full audited backlog H1-H19 + SEC-002 + M1-M6 + SEC-004 + SEC-003 is CLOSED — remaining security work = documented deferred LOW/class residuals only [SEC-001, C1, C9, H9-D1/10.5, H9-D2, modal-title audit, DG-3/4/5, HA-M5, cosmetics]).
- 2026-09-12 (XSS-C9): XSS-C9 → PARTIAL-FIXED (own-profile URL/media rendering surfaces — 2 files / 4 sink lines: profile.js:52 cover img src esc(cldUrl(...)) [the profile-view.js:267 M1-twin shape] + :125 initial-grid img src esc(cldUrl(...)) with the constant onerror preserved [the :501 twin] + :238 nav-avatar esc(url) + esc((PROF.username||'?')[0].toUpperCase()) data-fb transport + CONSTANT-expression onerror reading this.dataset.fb [ZERO attacker data in the JS string; esc transport chosen over encodeURIComponent — decodeURIComponent THROWS URIError on lone-surrogate letters, esc preserves parent behavior] + load-prof.js:27 the #nav-av INITIAL-render twin [stored profiles.avatar_url esc-wrap + the same transport — the nav-avatar SURFACE completion per the task's full-path audit; a documented C9 row site, the exploitable stored datum under the DB-write-bypass threat model]; threat model RE-AFFIRMED SELF-XSS stored, LOW preserved — 7-point verification: own row/session only, every cross-user renderer of the same columns already M1-escaped, no URL-param/realtime/import/localStorage cross-user vector [realtime = own row filter]; parent 09fd3d8 negative-control-proven vulnerable 366/0 — quote-break payloads MINT elements + hijack the keep-FIRST onerror and EXECUTE alert at :52/:125/:27 (the stored avatar_url vector), letter class = JS-string syntax breakout (fallback letter LOST), scheme payloads ride raw into the src VALUE (U3 entity-decode-back delivers javascript: as the decoded value — honest: inert at the img resource sink), no-quote markup honestly confined inside the quoted src value; post-fix focused 800/0 — every payload × every sink inert: zero minted elements/handlers, src VALUE confinement byte-exact round-trips, constant handlers only, data-fb letter round-trips EXACTLY incl. quote/backslash letters (functional improvement), ?t= cache-buster + transforms + empty-avatar branch + localStorage cover fallback preserved, benign cover/grid segments byte-identical parent vs disk; 322 regression 317/5 baseline-identical; app-load 10/10; all prior suites green [H1-H19, SEC-002, M1-M6, SEC-003, SEC-004] + 10 boundary harnesses exit 0 + secret scan CLEAN; sanctioned OUTSIDE-repo suite-tooling evolution only [M6 7 stale deferred-pins → C9-FIXED shapes 1383/0; H16 L.5 diff-bound +3 C9 transforms 449/0; NO in-repo harness changes — profile.js/load-prof.js already allowlisted]; NEW cross-user findings recorded NOT fixed per bounded-scope rule [dedupe rule #5 site additions, no new IDs]: universal-search.js:126 [AI-search ALL-users posts grid raw media_url/thumbnail_url — M1-class, the explore.js:70/:138 twins were fixed, this site missed] + story-highlights.js:37 [other users' highlights.cover_url raw img src — M1-class] on the XSS-M1 row + story-highlights.js:38 [highlights.title raw HTML text] on the H10-11 row; C9 row REMAINS PARTIAL: notes.js:57-58 + nova-ultra-patches.js:46 + memories.js:65 + avatar-action-sheet.js:11 stay OPEN accepted-low [different surfaces, not the task's 3 named surfaces]; SECURITY_ISSUES.md section 23 added + hooks renumbered 23→24).
- 2026-09-13 (XSS-M1 site additions): the three XSS-C9-audit-discovered cross-user deferred site additions → FIXED (universal-search.js:126 + story-highlights.js:37 + story-highlights.js:38/:39 [ledger-cited :38 = actual current :39, 1-line drift, sink unambiguous by content] — the dedicated cross-user universal-search/story-highlights task; 2 production files, 3 insertions/3 deletions: esc() wraps at all 3 AI-search media attribute boundaries [img thumbnail + video src + img media — the byte-exact explore.js:70 M1-twin shapes] + esc(cldUrl(h.cover_url, cover)) [the profile-view.js:267/:366 cover-twin shape] + esc(h.title||'Highlight') [HTML-text prescription]; NO sanitizeUrl on media src [resource sinks — the repo's established esc-only media pattern, every M1 twin; javascript:/data: values ride into the src VALUE inert at resource sinks, documented honestly]; NO dynamic handler changes [template onclicks = constant expressions with DB UUIDs]); threat model verified CROSS-USER STORED XSS (posts grid = ALL users' posts, NO user_id filter — proven via recorded db ops; receiver = any searcher; highlights = other-user profile modal via profile-view.js:193/:424/:474, receiver = any profile viewer; write paths = submit-create.js:96 client write + DB-write bypass [createHighlight is a toast-only stub; no in-repo schema constraints exist]; NOT downgraded to self-XSS; block-filter functional but not a security control); parent 3417b9b negative-control-proven VULNERABLE 236/0 (12 media payloads × 3 sinks + 14 cover payloads + 11 title payloads: quote-break mints + keep-FIRST handler hijack + alert(1) EXECUTION at all sinks — title ZERO-CLICK on modal open; honest classes: scheme value-rides inert at resource sinks, java&#x73;cript: entity decode-back, &quot; entity-quote decodes INSIDE the value [browser never re-tokenizes], no-quote markup confined; cross-user proof: attacker post/highlight renders in victim session with ALERT execution); post-fix focused 510/0 (zero minted elements/handlers/execution across the SAME matrix × all sinks; payload CONFINED as the exact src VALUE / exact TEXT round-trip; constant onclicks + template attrs preserved; cross-user post-fix inert; functional F1-F15 [Cloudinary grid_thumb/cover transforms exact, plain https + relative URLs unchanged, video raw+muted, 📷/✨/'Highlight' fallbacks, empty-states, create-button ownership, parent-vs-disk benign rendered-VALUE identity, db-error branch] + hygiene H1-H7 [1-line + 2-line diffs exactly, users-list segment byte-identical = H10-11 row untouched, esc load order, stubs unchanged]); 322 regression 317/5 (the documented origin/main-pin baseline; mid-task 5th signature = the documented dirty-worktree phase shift, self-resolves post-commit); app-load 10/10; all prior suites green [H1-H19, SEC-002 228/0, SEC-003 272/0, SEC-004 438/0, M1 894/0, M2-M6, M6 prove 533/0, XSS-C9 800/0]; boundary harnesses exit 0; node --check OK; secret scan CLEAN; VM load simulation 0 errors; in-repo harness change = branch2-only-safety allowlist + the 2 task production files ONLY (M1/SEC-004 precedent; failure signature unchanged); NO new issue IDs (dedupe rule #5 — the three sites were already recorded as site additions on the XSS-M1 + H10-11 rows); NEW findings: NONE; XSS-M1 index row → "site additions closed 2026-09-13"; H10-11 index row → "site addition FIXED; :111-:115 remain deferred"; SECURITY_ISSUES.md section 24 added + hooks renumbered 24→25 + §23.4 forward-pointer note + XSS-M1/H10-11 row notes updated + task report block appended).
- 2026-09-15 (H10-11): H10-11 → FIXED (cross-user users-list rendering — 1 file, 3 sink lines: universal-search.js:113 username [esc(u.username) — the H18 follow-list.js:34 twin] + :114 full_name [esc(u.full_name||'') — the H18 profile-view.js:88/:156 twin shape] + :115 bio slice [esc(u.bio.substring(0,60)) — substring-then-esc, ellipsis semantics on raw length unchanged; ledger-cited :111-:115 range, 2-line drift, sinks unambiguous by content — the row's own description names the three fields]; HTML-text esc() = the row's own prescription; NO sanitizeUrl [no URL context at these sinks]; NO handler changes [row onclick = constant with DB UUID, verified no client write path for profiles.id]); threat model verified CROSS-USER STORED XSS (profiles query has NO user_id scoping — proven via recorded db ops; bio = the ilike search-match field; write paths: saveEdit full_name/bio = NO validation [normal client write] + username = client-side regex only [DB-write bypass per task rule]; receiver = any searcher; NOT downgraded to self-XSS; block-filter functional but not a security control); every users-list field audited: :111 av(u.avatar_url, u.username, 46) = OPEN XSS-C1 row [untouched per task rule], :110 showUserProfile('${u.id}') = UUID-safe [H11-audit rows #6/#7, no client write path], :114 fmt(followers_count) = numeric-safe system counter [H11-audit row #9, H18 raw twins at profile-view.js:172/:176/:180/:395, no client write path — follows junction only; type-confusion bypass demonstrated + recorded as classification evidence, NOT fixed]; parent ba66895 negative-control-proven VULNERABLE 195/0 (exec-class payloads — markup + quote-breakout + data:text/html svg + long + Hindi/Cyrillic/CJK/emoji/ß — MINT elements + on* handlers and EXECUTE alert(1) ZERO-CLICK at all 3 sinks; mint classes incl. innerHTML <script> [browser-accurate non-execution, minting = the vuln]; honest classes: text/entity-text/mint-close/trunc/trunc-inert/exec-cut; cross-user proof: attacker row renders in victim session with ALERT execution); negative control 166 focused failures on the parent source = detection power; post-fix focused 466/0 (zero minted elements/handlers/execution across the SAME matrix × all sinks; payload CONFINED as exact TEXT round-trip incl. multilingual; cross-user post-fix inert with search functionality intact; functional F1-F15 [search opens, query shape, PEOPLE header, names/bio/followers render, avatar transports Cloudinary/https/relative/null, click opens correct profile, verified gate, empty state, AI header + input escaping, block filter, posts section + XSS-M1 media regression] + hygiene H1-H7 [3-line diff exactly, av/onclick byte-identical, esc load order, benign VALUES identical parent vs disk]); 322 regression 317/5 (git-stash parent-signature comparison: 4/5 byte-identical + branch2-final-readiness = the documented mid-task phase shift, self-resolves post-commit); app-load 10/10; VM load 0 errors; all prior suites green [H1-H19, SEC-002/003/004, M1-M6, XSS-C9 800/0, XSS-M1/USSH 511/0 + prove 236/0 — its H4/H5 task-boundary pins evolved to the H10-11-fixed shapes per the M6/H16 sanctioned outside-repo suite-tooling precedent]; 6 boundary harnesses exit 0; node --check OK; secret scan CLEAN; NO in-repo harness changes [allowlist already covers all commit files]; NO new issue IDs [dedupe rule #5 — the three sites are the row's own recorded fields]; NEW findings: NONE [followers_count type-confusion = classification note on the H10-11 row + §25.1, not a new ID]; H10-11 index row → FIXED; SECURITY_ISSUES.md section 25 added + hooks renumbered 25→26 + §24.1 forward-pointer + row note/status update + task report block appended).
- 2026-09-19 (XSS-C1): XSS-C1 → FIXED (the shared av() avatar helper — 1 file / the av() block only: src="${esc(displayUrl)}" [the M1 media-sink twin — img src is a resource sink, no sanitizeUrl] + data-fl="${esc(l)}" attribute transport with CONSTANT-expression onerror reading this.dataset.fl at runtime [the profile-view.js:392 pattern; esc chosen over encodeURIComponent — the latter throws URIError on the lone high surrogate of emoji-first names; exactly one escaping stage per boundary] + ${esc(l)} letter span + the dead safeName line REMOVED [H10-14]; 6 insertions/2 deletions, diff hunks verified confined to the av() block; ALL ~60 callers fixed at once INSIDE the helper — the audit's unit — no caller changes, no global replacement); threat model: CROSS-USER STORED XSS via profiles.avatar_url (DB-write bypass write path, ~40 cross-user receiver surfaces — universal-search users-list NO user_id scoping [H10-11-proven], feeds, other-user profiles, comments, DMs, calls, notes, notifications, admin lists; parent proof: attacker row renders in victim session with ZERO-CLICK alert execution) — the row's original LOW rating was premised on the 1-char letter vector (honest breakage class, SyntaxError not execution); the img-src quote-breakout M1-class vector was deferred INTO the row by the M1/H16/render-sv audits — both closed, upgrade documented in §26.1; parent 2391c749 negative-control-proven VULNERABLE 525/0 (exec payloads MINT elements + hijack keep-FIRST onerror + EXECUTE alert at every URL branch; mint classes incl. innerHTML <script> browser-accurate non-execution; honest classes: value-corruption, attr-pollution, scheme value-rides inert at resource sinks, entity decode-after-tokenization; letter classes drift the handler value — SyntaxError, no execution; cross-user end-to-end with the REAL av() — the H10-11 harness had stubbed av inert); negative control 412 focused failures on the parent source = detection power; post-fix focused 1431/0 POST-FIX SAFE (zero minted/handlers/execution across the full payload matrix × all 6 branches; payload CONFINED as exact decoded src VALUE; handler census identical to benign constant-expression control; data-fl letter round-trips exact incl. quote/backslash letters + emoji lone surrogate — no crash; legit Cloudinary transforms/plain https/relative/query-& preserved; fallback EXECUTION path fires cleanly, letters render exactly, ßule→SS expansion preserved; functional F1-F4 + hygiene H1-H7; cross-user row click → showUserProfile intact); 322 regression 317/5 baseline-identical (4× origin/main-pin + the documented mid-task phase shift); app-load 10/10; all 28 dedicated suites green; 5 boundary harnesses exit 0; VM load 0 errors; secret scan CLEAN; sanctioned outside-repo suite-tooling evolution ONLY (8 stale utils.js-untouched pins in H4/H6/H7/H14/SEC-003/SEC-004/C9/H10-11 verify harnesses → av()-strip/esc()-body shapes with dated comments, original invariants preserved; NO in-repo harness changes); H9-D4 + H10-3 + H10-14 rows → RESOLVED via XSS-C1; SECURITY_ISSUES.md section 26 added + hooks renumbered 26→27; NO new issue IDs (the ~60-call-site inventory re-confirmed one definition + the already-recorded adjacent rows — handle-incoming-call.js:12 = H9-D1/H9-D2 pre-existing; no site additions).
- 2026-09-19 (XSS-M7, Ban→Appeal task): XSS-M7 → FIXED (the ban→appeal chain status badges — load-appeals-list.js:20 ${a.status} admin appeals list + admin-tab-myapprovals.js:32 ${r.status} staff list, both now esc(); the status columns are client-insert-written [submit-ban-appeal / moderator-recommend-ban send status:'pending' in the client INSERT payload] — tampered-insert → stored cross-user XSS zero-click in the admin context; full 28-surface chain audit: every other sink inert at parent [appeal_reason/username/ban_reason/admin_notes esc'd; av() C1-hardened; M5 transports; notifications message esc'd :184; toast/prompt/confirm text-only; UUID onclick args DB-typed]; parent 97c8ecb proven vulnerable 379/0 + negctl 33 detection power; post-fix focused 398/0; 322 battery 317/5 baseline-identical; app-load 10/10; all 30 suites green; secret scan CLEAN; deferred same-class status sites [show-report-detail.js:189 + load-user-report-stats.js:32 reports.status; admin-tab-audit.js:112 audit status; load-admin-content.js:42/:44 media_type] + RLS runbook items [ban_appeals ownership/read-gating, ban_approvals moderator/admin gating, notifications type-'admin' sender-role verification] recorded in SECURITY_ISSUES.md §27.4 — NOT fixed in this commit per the task STOP rule; commit: Ban→Appeal harden, Branch2 only, main untouched df54898)
- 2026-09-21 (M7 §27.4 continuation task): XSS-M7 §27.4 sites → FIXED (the five deferred status/media_type sinks closed: show-report-detail.js:189 ${esc(r.status)} reporters-loop badge + load-user-report-stats.js:32 ${esc(r.status)} admin user-detail recent-reports badge [reports.status — client-insert-written: submit-report.js:8-13 ANY-USER client INSERT carries status:'pending' in the payload, recorded-op proven] + admin-tab-audit.js:112 ${esc(a.status)} audit statusBadge [audit_logs.status — write path RPC-ONLY: log-admin-action.js:7 log_audit_entry with CLIENT-SUPPLIED p_status param, ZERO direct client writes to audit_logs in src/, role-gating unverifiable from repo → site classified UNVERIFIED-external on the write side, render side hardened anyway] + load-admin-content.js:42 [${esc(item.media_type||'media')}] posts label + :44 [${esc(item.media_type||'media')} story] stories label [media_type client-insert-written: submit-create.js:94-102 posts / submit-story.js:133-139 stories — W8/W9 recorded-op evidence]; threat model: STORED CROSS-USER XSS in the ADMIN context — attacker files a tampered-status report / posts a tampered-media_type post or story → admin opens report-detail / user-detail / audit / content tab → zero-click alert(1); parent 828abab PROVEN VULNERABLE 362/0 (9 exec + 3-4 mint payload classes per site incl. multilingual/emoji; honest inert classes pinned; write-path evidence W5/W7/W8/W9 + R-blocks); negctl 63 expected failures = detection power; post-fix focused 373/0 (zero minted/handlers/execution; legit enums byte-identical — pending/resolved/dismissed/denied/failed/success-gate-absent/image/video/audio/unknown; complete admin workflows preserved end-to-end with REAL module flows); fix = exactly 5 esc() lines across exactly the 4 scoped files (S-block verified); 322 battery 317/5 baseline-identical (4/5 signatures byte-identical + documented mid-task phase shift); app-load 10/10; ALL 31 dedicated suites green (BA harness S5 later-task-worktree pin evolved per the H10/H11/H12 dated outside-repo precedent); boundary h9-byte/guard2/rollback exit 0 + guard1/dead_v2/validate_patches pre-existing stale failures stash-verified byte-identical at parent; node --check OK ×4; VM load 0 errors; secret scan CLEAN; NEWLY discovered deferred (NOT fixed per STOP rule): load-reports-list.js:84 messages.media_type + :89 stories.media_type + admin-tab-audit.js:113 audit_logs.actor_role — recorded §28.4 with DR1/DR2/DR3 proof; external DB dependencies recorded: log_audit_entry role-gating + §27.1 runbook items 1-4; SECURITY_ISSUES.md section 28 added + hooks renumbered 28→29; commit: M7 §27.4 status/media_type harden, Branch2 only, main untouched df54898)
- 2026-09-22 (§28.4 continuation task): XSS-M7 §28.4 sites → FIXED (the three deferred media_type/actor_role sinks closed: load-reports-list.js:84 [${esc(m.media_type)} message] admin reports-list message label [messages.media_type — CLIENT-INSERT-WRITTEN ×7 sites: send-media-msg.js:13 ternary + send-gif.js:13 / sticker-send.js:13 / upload-custom-sticker.js:15 'image' + toggle-recording.js:20 'audio' + share-location.js:13 'location' + complete-forward-message.js:22-24 verbatim source-copy propagation] + load-reports-list.js:89 [${esc(s.media_type||'media')} story] admin reports-list story label [stories.media_type — CLIENT-INSERT-WRITTEN: submit-story.js:133-139/:150/:156 + publish-story-editor.js:259/:273/:278] — both classified A. CONFIRMED VULNERABLE: attacker (any user) replays the app's own INSERT with tampered media_type (text falsy for :84 / media_url truthy for :89) → self-reports it (submit-report.js:8-13 ANY-USER client INSERT, NO self-report gate, proven from a NON-ADMIN session) → admin opens Reports tab → zero-click alert(1) in the ADMIN context; admin-tab-audit.js:113 ${esc(a.actor_role)} roleBadge [audit_logs.actor_role — write path RPC-ONLY: log-admin-action.js:7 log_audit_entry args = p_action_type/p_target_type/p_target_id/p_reason/p_status with NO actor_role param of any spelling, ZERO direct client writes to audit_logs in src/ → role value assigned server-side by the EXTERNAL RPC, unverifiable from repo → site classified D. UNVERIFIED DUE TO EXTERNAL DB/RLS/RPC DEPENDENCY, render side hardened anyway, external dependency retained §29.4]; threat model: STORED CROSS-USER XSS in the ADMIN context — attacker sends a tampered-media_type message or posts a tampered-media_type story → self-reports it → admin opens the Reports tab → zero-click alert(1); parent 3a27250 PROVEN VULNERABLE 231/0 (8 exec + 4 mint classes per media site incl. multilingual/emoji, 8 exec + 3 mint for actor_role; write-path evidence W2/W3/W4/W5/W6 + R-blocks); negctl 35 expected failures = detection power; post-fix focused 261/0 (zero minted/handlers/execution; legit labels byte-identical — image/video/audio/location/text/story/unknown media labels, '[media story]' fallback, '[Message deleted]'/'[Story expired]' gates, admin/moderator/staff/super_admin badges, user/null/undef/empty badge-absent gates; complete admin workflows preserved end-to-end with REAL module flows: full reports-list rows + filters + empty states + audit label/color maps + admin_actions fallback); fix = exactly 3 esc() lines across exactly the 2 scoped files (S-block verified); 322 battery 317/5 baseline-identical (all 5 failure signatures byte-identical to parent via git-stash comparison); app-load 10/10; dedicated suites 30/32 PASS with 2 documented era-pin evolutions (BA S5 worktree pin — resolves GREEN post-commit; M7S4 DR-era pin superseded BY DESIGN by this fix, preserved verbatim; both per the H10/H11/H12 dated outside-repo precedent); boundary h9-byte/guard2/rollback exit 0 + guard1/dead_v2/validate_patches pre-existing stale failures stash-verified byte-identical at parent; node --check OK ×2; VM load 0 errors; secret scan CLEAN; NEWLY discovered deferred (NOT fixed per STOP rule): admin-tab-audit.js:125 String(a.target_id).substring(0,8) raw (audit_logs.target_id, client-supplied p_target_id RPC param, 8-char truncation blocks tag+handler formation — LOW) — recorded §29.4; external DB dependencies recorded: log_audit_entry role-gating/p_target_id/actor_role + §27.1 runbook items 1-4; SECURITY_ISSUES.md section 29 added + hooks renumbered 29→30; §28.4 marked CLOSED; commit: SECURITY: harden remaining M7 status and actor role sinks, Branch2 only, main untouched df54898)
